const sendMatchData = (matchId, eventType, data) => {
    // Implementation of sending match data
};

const handleAPIErrors = (error) => {
    // Implementation of handling API errors
};

module.exports = { sendMatchData, handleAPIErrors };
